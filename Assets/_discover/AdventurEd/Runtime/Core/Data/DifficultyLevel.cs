namespace AdventurEd
{
    public enum DifficultyLevel
    {
        Default = 0,  // if set, we use the one defined by activities, or do not override anyway
        Easy = 1,
        Normal = 2,
        Expert = 3
    }
}
